# RAID Tracker

A command-line tool for logging Risks, Actions, Issues, and Decisions.

## Setup
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt

## Run tests
pytestgot